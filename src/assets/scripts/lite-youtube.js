// The <lite-youtube> video player used by the {% youtube %} shortcode
// (config/shortcodes/youtube-lite). Bundled from npm and served from this
// site, so embeds don't depend on a third-party CDN being reachable.
import '@justinribeiro/lite-youtube';
